{
  "name": "mcp-sentinel-ui-tests",
  "version": "1.0.0",
  "description": "Regression UI tests for MCP Sentinel Command Center",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.42.0",
    "@types/node": "^20.0.0"
  }
}
